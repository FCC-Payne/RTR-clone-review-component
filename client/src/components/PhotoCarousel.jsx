import React from 'react';

const PhotoCarousel = props => (
  <div>
    PhotoCarousel
  </div>
);

export default PhotoCarousel;

// get all photo urls for this dress
// for each photo url, render the photo (if a url is null, don't render)